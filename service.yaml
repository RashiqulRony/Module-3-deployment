apiVersion: v1
kind: Service
metadata:
  name: module3-service
  namespace: production
spec:
  type: NodePort
  selector:
    app: module3-app
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30080
